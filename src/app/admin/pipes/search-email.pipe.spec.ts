import { SearchEmailPipe } from './search-email.pipe';

describe('SearchEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
